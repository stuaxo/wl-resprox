# Debugging Scratchpad

## Crucial Environment Variables

When running apps or the proxy, these will be our best friends:

- `WAYLAND_DEBUG=1` - Dumps all Wayland protocol messages (Requests and Events) to stderr.
- `WAYLAND_DISPLAY=wayland-X` - Forces the client/proxy to connect to a specific socket.
- `RUST_LOG=debug` - Standard Rust tracing for our proxy logic.

## Tools

- `wayland-info` - Good for verifying which globals our proxy is successfully passing through.
- `wev` - Wayland event viewer. Good for testing if keyboard/mouse inputs are surviving the proxy translation.
- `strace -e trace=sendmsg,recvmsg -s <N> -o <file>` - syscall-level ground truth for "what bytes actually crossed the wire," used repeatedly earlier in this project's history (the new_id-gap bug, the partial-write bug) to settle disagreements between what we *believe* we sent and what a real peer *received*. Not installed in the container image by default (`apt-get install strace` as root) -- reach for it when log-level (`RUST_LOG=debug`) evidence alone isn't conclusive, e.g. a real compositor genuinely not responding vs. a hang on our own end.

## Known Edge Cases to Watch Out For

- (To be filled: Track specific ID mismatches, memory leaks, or missing configure events here as we test)
- Buffer lifetimes: GTK might try to release a `wl_buffer` that the new compositor instance doesn't know about yet.

## Ad-hoc Observations & Experiments

- Host `gnome-shell` restart behavior: Running `pkill gnome-shell` on the host machine caused the login screen to temporarily disappear, followed by a momentary flash of existing windows before the shell fully recovered.
  - Hypothesis: The display server (Mutter) might have held onto the framebuffers briefly, or Xwayland apps survived. We need to verify if native Wayland GTK sockets actually dropped during this event.

- 2026-07-29: Chased down the `/dev/dri/renderD128: Permission denied` blocker for `gtk4-demo` inside the nested labwc. Root cause turned out to be two separate, stacked problems — device permissions were never actually the whole story:

  1. **`sudo` inside the rootful container required a real TTY, blocking all non-interactive automation.** The container ships `sudo-rs` (Ubuntu 26.04's Rust reimplementation of sudo), which enforces `Defaults use_pty` unconditionally — even with a `NOPASSWD` rule in `/etc/sudoers.d/`, `sudo` still refused with "a terminal is required to authenticate" when run from a session with no controlling tty. Commenting out `Defaults use_pty` in `/etc/sudoers` (not just adding NOPASSWD) was required to unblock non-interactive `sudo`. A `NOPASSWD` drop-in file also has to sort *after* the existing `/etc/sudoers.d/sudoers` file alphabetically (e.g. `zz-*`, not `90-*`) — sudoers uses the last matching rule, and the stock file's `%sudo ALL=(ALL:ALL) ALL` (no NOPASSWD tag) comes after a numerically-prefixed drop-in and silently re-requires a password.

  2. **`podman exec --user=stu` (what `distrobox enter --root` does under the hood) does not apply supplementary/secondary groups from `/etc/group` at all** — it only sets the primary GID from `/etc/passwd`. So `usermod -aG video,render stu` run after container creation is silently ignored by every subsequent exec session: `id stu` (an NSS lookup) correctly listed `video`/`render`, but the *actual running process's* `id` (no argument) did not, and `/dev/dri/renderD128` stayed `Permission denied` even though the device's own ownership (`root:render`, mode `0660`) and `stu`'s `/etc/group` membership were both correct. Explicitly overriding the group at exec time — `podman exec --user stu:render ...` — works reliably.
     - Tried the "proper" fix: recreating the container with `--group-add 991 --group-add 44` baked in at `distrobox create` time (now in `scripts/setup-env.sh`). Verified in isolation on a **bare** `podman run --group-add ... --privileged` container that this does make `exec --user=<name>` (no override) pick up full supplementary groups correctly, including newly-`usermod`'d ones.
     - It does **not** work the same way inside a distrobox-managed container, though — confirmed with a completely fresh test user (`useradd` + `usermod -aG render`, no distrobox-specific setup) created *inside* `wayland-proxy-dev` after the group-add fix: still only got `groups=<primary>,0(root)` on exec, identical to `stu`. `/etc/group`, `/etc/gshadow`, and `/etc/passwd` all looked structurally normal (checked with `cat -A`, no encoding/formatting issues), and a container restart didn't change anything. This looks like a genuine, unresolved podman/distrobox interaction bug rather than a config mistake on our end — not root-caused further since the explicit-override workaround is reliable. Worth revisiting if this becomes annoying enough to chase properly.
     - **Practical upshot: `distrobox enter --root wayland-proxy-dev` alone still does NOT get GPU access.** Anything needing `/dev/dri` (nested labwc, GTK apps rendering via EGL) must be invoked with the explicit group override, e.g.:
       ```
       sudo podman exec --user stu:render wayland-proxy-dev <command>
       ```
       (`playbook.yml` still sets up the `video`/`render` groups properly via Ansible — that part is correct and necessary, just not sufficient on its own for `distrobox enter` sessions specifically.)

  - Once both were worked around, `labwc -C /dev/null` started cleanly (new `wayland-N` socket, no errors) and `gtk4-demo` ran against it with no permission/EGL errors — only benign `Portal operation not allowed` warnings from the missing `xdg-desktop-portal` in this minimal headless setup.

- 2026-07-29 (follow-up): Root-caused *why* `sudo` as `stu` never has a working password in the first place — this was the thing actually catching manual attempts to run `scripts/setup-env.sh` interactively ("caught by the password thing again"). It's not a "set a password on first entry" step at all (the old comment in `setup-env.sh` claiming that was wrong/never verified) — there is no such interactive step in distrobox's tooling.

  What actually happens: distrobox's own container-init script (`/usr/bin/entrypoint`, baked into the image, not our code) unconditionally clears `stu`'s password on first container boot (`chpasswd -e` with an empty encrypted field — shows as `passwd -S stu` → `NP`, "no password"). For rootful containers it then just locks `root` (`usermod -L root`) and stops there — it never leaves `stu` with a working credential *or* a `NOPASSWD` sudoers rule. Net effect: the very first `sudo` call as `stu` (e.g. `setup-env.sh`'s own `apt-get`/`ansible-playbook` step, or any manual `sudo` in an interactive `distrobox enter --root` shell) hits a real `[sudo] password for stu:` prompt that **no password can satisfy**, because none was ever validly set. Typing anything — including guesses, blank enter, the host's own password — fails identically, which is exactly the "maybe I'm setting it to something unknown" symptom.

  Fix, in the standard "disposable dev container" style (same pattern most devcontainer base images use for their default user):
  - `scripts/setup-env.sh` no longer goes through `distrobox enter --root` (as `stu`) for provisioning at all — it now runs `apt-get`/`ansible-playbook` directly via `sudo podman exec <container>` (container root, no `--user`), sidestepping `stu`'s broken sudo entirely for automation. (Also had to add an explicit `sudo podman start` before this — `distrobox create` only creates the container, doesn't start it, and a short poll loop waiting on `podman exec ... true` before the container's own init/apt-get finishes.)
  - `playbook.yml` has a new task, `Allow stu passwordless sudo`, that writes `/etc/sudoers.d/zz-stu-nopasswd` (`stu ALL=(ALL) NOPASSWD: ALL`, validated via `visudo -cf` — confirmed sudo-rs supports `-cf` on a specific file, not just the main `/etc/sudoers`). Filename deliberately sorts after the stock `sudoers` drop-in for the same last-matching-rule reason noted above. This is what actually fixes things for *your own later interactive* `sudo` use inside `distrobox enter --root` — confirmed working (`sudo true` succeeds, no prompt, even though `passwd -S stu` still correctly shows `NP`).
  - Verified the full fresh-container flow non-interactively end to end after this fix: `setup-env.sh` (9/9 ansible tasks OK) → `start-host.sh` → `start-guest.sh` (nested `labwc` starts clean, no permission errors) → `gtk4-demo` renders with only the same benign portal warnings as before.

- 2026-07-30: Root-caused the intermittent `wl_display.error: invalid arguments for wl_registry#2.bind` bug that had survived the partial-write fix, the busy-loop fix, and disproved both the message-batching and message-pacing theories (see earlier same-day entries above this one in the raw log -- summarized here since this is the entry that actually closes it out).

  **Root cause**: not a wire-encoding bug at all. labwc/wlroots (via libwayland-server) requires a client's `new_id`-allocating requests to arrive without gaps. Our relay silently *drops* any request on an object whose interface isn't in `interfaces.rs`'s lookup table (deliberately, to avoid misattributing fds on a message we can't parse -- see that file's doc comment). Real clients routinely send such a request immediately after binding a global we don't recognize (e.g. `zwp_linux_dmabuf_v1.get_default_feedback` right after binding `zwp_linux_dmabuf_v1`) -- and if that dropped request carried a `new_id`, the compositor never learns about that id, leaving a gap. The *next* new_id-bearing message we forward (another bind, a `sync`, whatever) then gets rejected as "invalid arguments," even though it's byte-perfect on its own.

  Confirmed with a small throwaway diagnostic client (`examples/probe_bind.rs` -- connects as a real Wayland client, binds a chosen set of globals in one burst, checks for `wl_display.error`) run against real labwc:
  - Binding several recognized-and-unrecognized globals together, no interleaved follow-up requests: always passes.
  - Same binds + an interleaved follow-up on an unrecognized interface (which we drop): fails.
  - Same binds + an interleaved follow-up on a *recognized* interface (which we forward): passes.
  - A bare client-side new_id **skip** with zero proxy involvement at all (`SKIP` token in the probe, burns an id, sends nothing): fails identically. This isolated it conclusively -- it's the gap itself, not our drop logic specifically, that labwc objects to.

  **Fix**: expanded `interfaces.rs` coverage so far fewer real-world globals hit the drop path at all. Added three more crates from the same smithay/wayland-rs family -- `wayland-protocols-wlr` (wlroots' own extensions: layer-shell, screencopy, gamma-control, etc. -- never went through freedesktop's official process, hence a separate crate from `wayland-protocols`), `wayland-protocols-plasma` (KDE's `org_kde_kwin_*`), and `wayland-protocols-misc` (orphaned-but-widely-used ones that fit neither bucket: `zwp_input_method_manager_v2`, `zwp_virtual_keyboard_manager_v1`) -- plus enabled `wayland-protocols`' own `staging`/`unstable` features, which were off. Went from 7 resolvable interfaces (core wayland.xml + xdg-shell only) to 46. Only top-level bindable globals need a table entry; child objects created by typed requests (e.g. `get_default_feedback` -> `zwp_linux_dmabuf_feedback_v1`) resolve automatically through that request's static `MessageDesc::child_interface`, never through this table.

  Added `interfaces::tests::resolves_every_global_observed_from_real_labwc`: a hardcoded list of every global a real run against labwc 0.9.3/wlroots 0.19.2 actually advertised, asserting each resolves. This is a plain `cargo test`, no live compositor needed -- it's meant to fail loudly the next time a real desktop's global list outpaces this table, instead of the silent per-message drop that caused this whole bug.

  Verified: all 18 tests pass; the exact probe scenario that reproduced the bug now passes; 15/15 clean `wayland-info` runs against real labwc with zero warnings/errors in the proxy log (previously deterministic 15/15 *failure* right before this fix).

  For the standing design of `interfaces.rs` itself -- what it covers, the gap-1/gap-2 framework, what's still open, and what improvements were considered but deferred -- see `docs/architecture-notes.md` rather than duplicating that here; this entry is the investigation log, that file is the reference that outlives it.

- 2026-07-30: First live-recovery test against real labwc (`shadow-table-1to1` branch's reconnect work, tasks 6-13 all passing in isolation via fake-compositor tests) hit a genuine hang: `recover_state_after_reconnect` sends `get_registry`+`sync` to the freshly-reconnected real compositor and never observes a response, stuck indefinitely (confirmed via `RUST_LOG=debug` -- "compositor reconnected -- recovering state" logs, then nothing for 30+ seconds, `gtk4-demo` still alive but frozen forever instead of recovering).

  **Crash method used**: `kill -9` on the `labwc` process directly (not `scripts/test-crash.sh`, which does the same thing but bundles it with its own harness). Then a *second* `labwc -C /dev/null` process started manually, targeting the same socket name the first one used (`wayland-0` in this run) after removing its stale socket/lock files, to simulate "the same compositor restarting" rather than a supervisor picking a fresh display name.

  **Why this matters going forward**: `kill -9` is the crudest possible crash -- instant, no cleanup, guaranteed `ECONNRESET`/EOF on the socket. It's not the only real-world failure mode (a compositor could also hang without closing the socket, crash mid-write leaving a partial message, or exit cleanly). Worth eventually formalizing a small matrix: {crash method} x {how the proxy is expected to detect it} x {which debugging tool actually shows what's happening} -- rather than continuing to improvise one scenario at a time. Not done yet; noting the gap rather than solving it now.

  Root cause of the hang itself not yet found -- next step is `strace` on the proxy process (see Tools above) to see whether `get_registry`/`sync` actually leaves our socket at all, since all the fake-compositor integration tests that exercise this exact code path pass reliably, meaning either something about a *real* compositor's response differs from our fakes, or something about this specific container/socket setup (freshly rebuilt Containerfile-based container, not yet proven for this exact scenario) is the actual variable.

- 2026-07-30 (follow-up): `strace -f` on the proxy binary itself (wrapping from launch, not attaching -- `ptrace(PTRACE_SEIZE, ...)` on an already-running PID fails with `Operation not permitted` under this container's yama ptrace_scope) found three real bugs, all in `shadow-table-1to1`, none caught by any fake-compositor test:

  1. `ShadowTable.next_host_id` never reset across reconnects. A fresh host connection is a brand-new `wl_client` to the compositor, requiring gapless allocation from 2 -- same rule as the earlier new_id-gap bug, just via the allocator this time. First real message after reconnect used some arbitrary high id (161 in the reproducing run); labwc rejected `get_registry` outright and closed the socket.
  2. The registry-collection loop's `host.fill().await?` didn't check for `Ok(0)` (EOF) -- `?` doesn't catch it, it's not an `Err`. Once (1) made labwc close the connection, this spun at 100% CPU calling `recvmsg` forever (2.6M+ lines in the trace).
  3. Fixing (1) safely required making `host_id`/`guest_id` generation-aware (reject stale mappings that could numerically collide with fresh ones -- both compositor instances' allocators restart from the same baseline). That broke `wl_display`'s own 1:1 mapping, seeded once at `ShadowTable::new()` and never touched again -- every message references `wl_display` via `sync`/`get_registry`/`delete_id`, so this broke everything. Caught by the existing `proxy_reconnects_to_a_restarted_compositor` test failing, not by live testing.

  Fixed: reset `next_host_id` to 2 and re-seed `wl_display`'s mapping in `bump_generation()`; check `fill()`'s return value explicitly. Four new regression tests. Commit `9d2dc8f`.

- 2026-07-30 (follow-up): with the above fixed, a full live run got further but still failed: recreation succeeded (fresh gapless host ids 4-9), but the client's `ack_configure` for our *synthesized* `xdg_surface.configure` (see `recover_state_after_reconnect`, sent straight to the client to force a repaint) forwarded to the real compositor like any other message -- which rejected it, `"wrong configure serial: 1"`, since it never issued that serial itself. Second EOF followed immediately, and this time `gtk4-demo` disconnected instead of surviving.

  Not a wire-encoding or allocator bug this time -- a genuine gap in the recovery design: nothing reconciled an invented client-side serial against the real compositor's own serial tracking. `implementation-constraints.md` says to synthesize the configure, not how to handle the resulting ack.

  **Fix**: `pending_configure_acks: HashMap<u32, u32>` (guest `xdg_surface` id -> pending synthetic serial), threaded through `run_connection` alongside `objects`/`graph`/`grabs`. `recover_state_after_reconnect` records the entry when it sends the synthetic configure; `relay_ready_messages` checks it on every `ClientToHost` `xdg_surface.ack_configure` and, on a match, consumes the entry and drops the message instead of forwarding it. Regression test extended onto `full_reconnect_recreates_surface_chain_and_synthesizes_configure` (asserts the second life's next observed message is the one sent *after* the ack, proving the ack itself never arrived).

  Verified live end to end: crashed labwc, started a replacement on the same socket, watched the proxy recreate the full chain, synthesize+ack the configure without a compositor error, receive a second *real* `xdg_surface.configure`/`ack_configure` pair from the new compositor normally, and `gtk4-demo` stayed alive and connected throughout -- task 14 (live-verify crash recovery) passes.

- 2026-07-30: First real use of `scripts/start-host.sh` + `scripts/start-guest.sh`'s documented two-layer flow (nested `labwc` inside the container, attached to a host `labwc`+`wayvnc` session) -- every prior live test had used a single headless `labwc` directly inside the container instead, so this path had never actually been exercised end to end. Failed immediately: `entrypoint.sh`'s nested `labwc` errored `Could not connect to remote display: No such file or directory`.

  **Root cause**: `scripts/setup-env.sh` never mounted the host's `XDG_RUNTIME_DIR` (`/run/user/<uid>`, where `start-host.sh`'s Wayland socket lives) into the container -- only `$HOME` and `/dev` were shared. The container's own `/run/user/<uid>` was a separate, empty directory created from scratch. Distrobox (the setup this replaced) forwarded the host display socket automatically; the plain-podman replacement never reproduced that specific piece.

  **Fix**: `setup-env.sh` now bind-mounts `/run/user/<uid>` at the same path in the container, alongside a preflight check that the directory exists on the host first. This also made the old root-exec `mkdir`/`chown`/`chmod` step (which created the container's *separate* runtime dir) unnecessary -- removed, since the bind-mounted host directory already has correct ownership by construction (container UID is built to match the host's).

  Verified: recreated the container, confirmed the host's `wayland-0` socket is visible inside it, re-ran `start-guest.sh` -- nested `labwc` connects and creates its own `wayland-N` socket successfully.

- 2026-07-31: While fixing a container-shell prompt that was indistinguishable from the host's own (`hostname` returns the same value on both sides -- `--network host` shares the UTS namespace too), baking a `PS1` customization into the image's `~/.bashrc` had no effect at all. Root cause was bigger than the prompt: `setup-env.sh` mounted `-v "$HOME:$HOME"` -- the *entire* host home directory, not just the project -- so on this dev host, where the host account happens to also be named `stu` (matching what the container's own user was then hardcoded to), the bind mount completely shadowed the image's `/home/stu` with the host's real one. Anything baked into that user's home directory in the image was silently discarded at runtime, and -- the more serious half -- anything the container wrote there would have landed in the real host home directory.

  **Fix**: stopped mounting `$HOME` at all. `setup-env.sh` now mounts only the project directory, at a fixed generic path (`/workspace`) inside the container, matching a standard devcontainer layout. The container's own user was also renamed `stu` -> `dev` (a fixed, generic login name -- only the numeric UID/GID need to match the host, never the username) so nothing about the container depends on whoever's account happens to be building it. Every script that previously computed a host-side absolute path and reused it as a container-side one (`start-guest.sh`'s `entrypoint.sh` invocation, `diagnose.sh`'s self-invocation inside the container) now references the fixed `/workspace` path instead.

  Once `$HOME` genuinely wasn't shared, the original `~/.bashrc` PS1 attempt still didn't work, for an unrelated reason: Ubuntu's own stock `~/.bashrc` sets `PS1` itself further down, after anything sourced earlier (confirmed by first trying `/etc/bash.bashrc`, verifying with a marker variable that it *was* being sourced, and finding Ubuntu's `~/.bashrc` simply overwrote `PS1` afterwards regardless). Fixed by appending to `~/.bashrc` directly so it runs last.

  Verified: rebuilt the container, confirmed `/workspace` holds the project and `$HOME` (`/home/dev`) holds only the stock two-file skeleton, confirmed the host's real `~/.bashrc` is untouched (identical byte for byte before and after), confirmed `cargo build` and the full `start-guest.sh` flow (entrypoint, nested `labwc`, interactive shell landing in `/workspace` as `dev`) all still work, confirmed the `PS1` prefix renders.

- 2026-07-31: Followed up on the intermittent `test-crash.sh` slowdown/failure from the X11-contention investigation (see `diagnose.sh`'s rewrite entry) by disabling XWayland entirely for our own headless test/nested compositor instances -- this project never uses XWayland, so removing it was a real hardening improvement regardless of outcome, not just a debugging experiment.

  Added `scripts/labwc-config/rc.xml` (`<core><xwayland>no</xwayland></core>`), wired into `entrypoint.sh` and `test-crash.sh` (not `start-host.sh`, which runs a real interactive session and should keep labwc's normal config search). First attempt silently did nothing: the explanatory comment inside the XML used `--` (em-dash-style) punctuation, which XML forbids anywhere inside a comment body -- labwc's parser rejected the whole file and fell back to default config with no visible warning beyond a one-line parse error easy to miss in the log. Fixed the punctuation, confirmed live afterwards: no `Xwayland` process spawns at all now (previously left zombie `Xwayland` processes behind every run).

  **However**: this did not fix the underlying intermittent failure. Ran `test-crash.sh` four times in a row with XWayland confirmed fully disabled -- all four still failed the same way, `labwc` alive but never creating its own socket within the timeout. `fuser -v /dev/dri/renderD128` shows the real bottleneck: this host's GPU render node is shared with other, unrelated `labwc`/`Xwayland` sessions outside this project entirely, same ones found in the earlier X11-contention investigation. That's GPU/DRM-level contention, not XWayland-socket contention -- a genuine property of this specific shared host, not something fixable from inside this project. Not chasing further: `diagnose.sh`'s X11-contention view already surfaces this class of problem when it recurs, and `test-crash.sh`'s 20s timeout (already bumped once) stays as a reasonable-effort accommodation, not a guarantee.

- 2026-07-31 (correction): the "no `Xwayland` process spawns at all now" confirmation above doesn't reproduce after rebuilding the container from scratch (Phase 9's multi-WM restructuring) -- same `rc.xml`, byte-identical even when copied fresh to a new path, `Xwayland` still spawns. The XML itself parses fine this time (no parse-error line), so it isn't a repeat of the punctuation bug either.

  Best guess: the original "confirmed" run coincided with X0 *and* X1 both already being exhausted by the other unrelated sessions on this host, and what actually suppressed `Xwayland` that time was resource exhaustion, not `xwayland=no` taking effect -- i.e. likely a false positive, not a verified fix. Didn't chase the exact rc.xml schema down further (checked `strings` on the `labwc` binary for the real parser key, inconclusive) -- this setting was already established as *not* fixing the real GPU-contention flakiness above, only a secondary log-noise/process-count cleanup, so not worth more time against a moving, shared-host target. Leaving the config in place (harmless either way); not claiming it's confirmed effective anymore.

- 2026-07-31 (the actual root cause): the "GPU/DRM contention" conclusion two entries up was wrong, or at least not the main story. Chasing what looked like the same flakiness during Phase 9's multi-WM restructuring, `labwc -C ...` run directly (bypassing `test-crash.sh`) showed a real compositor starting cleanly and logging `WAYLAND_DISPLAY=wayland-0` -- a socket `test-crash.sh` insisted never got created.

  **Root cause**: `test-crash.sh`'s own socket-detection logic (see its earlier 2026-07-30 fix entry) snapshotted socket *names* before starting the compositor, then looked for a name absent from that snapshot. That fix correctly handled a stale dead socket being mistaken for the live one. It didn't handle the opposite case: `wayland-0`'s socket *file* was still sitting in `$RUNTIME_DIR` (left behind when the host's own `start-host.sh` `labwc` was killed earlier in this exact debugging session), unlocked, so wlroots' own auto-selection legitimately reused the name `wayland-0` for the new compositor -- and because that name was already present in the "before" snapshot, the diff never flagged it as new. Every "compositor never created a socket" failure chased across the last several entries was most likely this, not host GPU contention -- the GPU-contention evidence (`fuser` showing other sessions on `renderD128`) was real, but conflated with a separate, actually-in-our-control bug.

  **Fix**: snapshot `(name, live-pid)` pairs instead of names alone (`fuser` each candidate socket). A name only counts as "the new compositor" if it's live now and wasn't live under that same pid before -- correctly handles both a stale dead name being ignored *and* a stale name being legitimately reclaimed.

  Verified: 3/3 clean `SUCCESS` runs immediately after the fix, on the exact same host, no change to GPU/host load between attempts.

- 2026-07-31: Phase 9's sway container passed L0 (`test-crash.sh`, 5/5). The L1 check (manual crash+restart with `RUST_LOG=debug`, checking logs rather than just process survival) surfaced a real gap: `gtk4-demo`'s window never appeared, even though the proxy didn't crash and the client stayed connected. The proxy logged `wl_registry.bind would create an object with unresolvable interface -- dropping` for one global partway through the client's bind sequence, and `get_toplevel` never appeared in the log afterwards (0 occurrences) -- the same new_id-desync bug class as the original labwc-era gap (2026-07-30 entries above), just triggered by a different missing interface this time.

  **Root cause**: `src/interfaces.rs`'s static lookup table never covered `zwp_keyboard_shortcuts_inhibit_manager_v1`. sway advertises it; the labwc instance the table was built against apparently doesn't (or gtk4-demo never tried to bind it there) -- exactly the "new compositor family surfaces a new global" scenario `plan-test-harness.md`'s Phase 9 anticipated, not a coverage oversight from carelessness. Found by capturing the full `wl_registry.bind` stream, decoding the hex-encoded interface name/new_id bytes around the WARN, and cross-referencing against a full `wayland-info` dump of sway's own global list.

  **Fix**: added the interface (`use wayland_protocols::wp::keyboard_shortcuts_inhibit::zv1::client::__interfaces as keyboard_shortcuts_inhibit;` + the matching lookup-table arm), following the existing ~35-alias convention exactly. Confirmed the guessed module path (gated behind the crate's `unstable` feature, already enabled in `Cargo.toml`) and constant name (`ZWP_KEYBOARD_SHORTCUTS_INHIBIT_MANAGER_V1_INTERFACE`) were both correct on the first `cargo build` -- no trial and error needed.

  Verified live end to end against sway: rebuilt, crashed sway mid-session, restarted it on the same socket -- `get_toplevel` now appears (was 0, now 1), zero "unresolvable interface" warnings, full toplevel chain (`wl_compositor` -> `wl_surface` -> `xdg_wm_base` -> `xdg_surface` -> `xdg_toplevel`) recreated cleanly on reconnect, client alive throughout. `cargo test` and 5/5 `test-crash.sh` runs against sway both still pass.

- 2026-07-31: First cross-compositor swap test (`plan-test-harness.md`'s labwc -> sway pair) -- proxy on the host, `gtk4-demo` client in the sway container, labwc crashed and sway brought up on the same freed `wayland-N` name it left behind (same reuse mechanism `test-crash.sh` relies on, just driven across two separate containers sharing the host's bind-mounted `XDG_RUNTIME_DIR` this time). Passed on the existing L1 criteria: client survived, full toplevel chain recreated, zero "unresolvable interface" warnings.

  It also surfaced a different class of WARN never checked for before: `wl_surface.attach references untranslatable object 83 (ClientToHost) -- dropping`, matching `set_acquire_point`/`set_release_point` drops on the same guest ids, plus `delete_id for untracked host id 3 -- ignoring`. First read as a swap-specific finding -- looked like exactly the "protocol-strictness differences between implementations" risk this section of the plan called out.

  **Turned out not to be swap-specific.** A same-compositor control (labwc -> labwc, same ~15s pre-crash idle window) reproduced the identical warnings, same guest ids, same host id 3. Root cause: `gtk4-demo` uses explicit sync (`wp_linux_drm_syncobj_v1`) and had a buffer + two sync timeline objects mid-flight when the crash landed -- ordinary per-frame double-buffering churn, not part of the `RecreationGraph`. Any generation bump invalidates guest ids outside that graph (by design -- see `implementation-constraints.md`'s "Buffer Lifetimes" rule, which already covers the reverse direction, `wl_buffer.release`). The client trying to reuse a pre-crash buffer/timeline id post-reconnect is the same story, just `ClientToHost`. The proxy does the correct thing either way: drop the message, log it, don't crash, don't disconnect the client.

  Genuinely open question this narrow (L1, unattended) test can't answer: whether dropping that in-flight `wl_surface.attach` leaves the client's *next visible frame* stuck rather than just delaying it -- exactly what L1 vs. L2 in this plan's testing-levels table distinguishes. Not chased further here; flag for an L2 (VNC-watched) pass if it matters before Phase 10. `delete_id for untracked host id 3` also reproduced identically in the control, so it's likewise a generic post-generation-bump artifact, not a sway/labwc protocol-strictness gap -- no interfaces.rs-style fix applies here, there's nothing to add.

- 2026-07-31: Phase 9's kwin/plasma container. Per the plan's own instruction ("check KDE's own headless-CI setup before inventing one"), confirmed live via `kwin_wayland --help` that `--virtual` is exactly that -- Qt's own headless framebuffer backend, the direct analogue of wlroots' `WLR_BACKENDS=headless`. No web search turned up the exact flag reliably; running `--help` inside the actual container (once it would start at all, see below) was faster and more trustworthy than guessing from KDE CI docs/blog posts.

  Two build-time failures before kwin would even start, neither a protocol issue:

  1. `groupadd -g "${RENDER_GID}" render` failed with "group 'render' already exists" -- one of kwin-wayland's own transitive deps (unlike sway/labwc) creates a system `render` group during its own postinst. Fixed with the same groupmod-with-fallback pattern the Containerfile already used for `video`.
  2. Once the image built, `kwin_wayland` failed to *execute at all* -- plain shell `Operation not permitted`, not a runtime error. `getcap` showed `cap_sys_nice=ep` baked into the binary (for realtime scheduling on a real desktop); execve() of a capability-carrying binary fails outright in this rootless-podman user namespace, since the process's capability bounding set can't satisfy it. This is a completely different failure mode from the video/render GID mismatches seen before (those are `/dev/dri` permission errors, EACCES/EPERM *after* a successful exec, not exec itself failing) -- worth remembering as a distinct class of container/capability issue for any future WM whose binary ships file capabilities. Fixed with `setcap -r /usr/bin/kwin_wayland` at build time; the only visible cost is a non-fatal "Failed to gain real time thread priority" log line at startup, irrelevant for a headless test compositor.

  L0 (`test-crash.sh`) passed 5/5 once the container would run at all.

  L1 (manual crash+restart, `RUST_LOG=debug`) surfaced five new `interfaces.rs` gaps in one run: `xdg_wm_dialog_v1`, `wp_color_representation_manager_v1`, `xdg_system_bell_v1`, `wp_color_manager_v1`, `wp_fifo_manager_v1` -- all dropped with "wl_registry.bind would create an object with unresolvable interface -- dropping" during gtk4-demo's startup bind sequence. The existing warning didn't log *which* interface was unresolvable (only the request's own interface/method, `wl_registry.bind`), which made this harder to diagnose than the sway gap -- fixed the warning itself first (`src/lib.rs`, now logs `walk.dynamic_interface_name`) before chasing the actual gaps, since it's a permanent diagnostic improvement, not a one-off print-debug.

  All five confirmed Gap 1 (per `architecture-notes.md`'s framework): already generated by `wayland-protocols`' `staging` cargo feature (already enabled), just never wired into the lookup table -- confirmed by grepping the crate's bundled protocol XML directly (`xdg-dialog-v1.xml`, `xdg-system-bell-v1.xml`, `color-management-v1.xml`, `color-representation-v1.xml`, `fifo-v1.xml` all present) rather than assuming. None of these are wlroots protocols; they're Plasma-6-era staging extensions no labwc/sway run ever surfaced -- exactly the "new compositor family, new globals" pattern the plan anticipated, not an oversight. Fixed the same way as the sway gap: five new aliases + match arms, following the existing convention.

  Re-verified live end to end after the fix: zero unresolvable-interface warnings, `get_toplevel` reached and the client stayed up, then a real crash+restart cycle (`kill -9` the compositor, start a fresh `kwin_wayland --virtual`) recreated the full toplevel chain (`wl_compositor`, two `wl_surface`s, `xdg_wm_base`, `xdg_surface`, `xdg_toplevel`) cleanly, client alive throughout. Confirmed as a side effect, not assumed: kwin's own socket auto-selection reused the exact same freed `wayland-N` name across the crash, the same reuse behaviour `test-crash.sh` and the labwc->sway swap test both depend on -- this isn't wlroots-specific, apparently just how the generic Wayland socket-binding convention (`wl_socket_lock`-style advisory locking on a shared naming scheme) tends to get implemented. `cargo test` (including two new regression tests, `resolves_every_global_observed_from_real_kwin` mirroring the existing labwc one) passes.

- 2026-07-31: Phase 9's mutter/gnome-shell container -- the plan's own risk callout ("needs more session stack ... than labwc/sway") turned out accurate, but the actual blocker was narrower than "needs a real systemd user session."

  Headless invocation: `gnome-shell --headless --no-x11` (confirmed live via `--help`; `--no-x11` matters because gnome-shell spawns a full Xwayland + ibus + a large D-Bus service fan-out by default otherwise -- confirmed via `pstree`). Build hit the same GID-collision class of error kwin's Containerfile already had a fix for, but for a genuinely different reason: this host's `RENDER_GID` (991) happened to already be occupied by an unrelated pre-existing group `clock` (GNOME-stack-installed, nothing to do with video/render), so kwin's groupmod-with-fallback (retry as `groupadd` if the target group doesn't exist yet) wasn't sufficient -- `groupmod` failed because the target GID was taken by the *wrong* group, not because `render` was missing. Generalized fix: detect whatever group currently holds the target GID and relocate it (to `gid + 50000`, an arbitrary unused value -- nothing in the image depends on its identity) before groumod'ing/creating the target group either way.

  Once built, `gnome-shell --headless --no-x11` created its Wayland socket successfully but then `Aborted (core dumped)` almost immediately, before a client could connect -- `Gjs-CRITICAL** JS ERROR: Gio.IOErrorEnum: Could not connect: No such file or directory` inside `timeLimitsManager.js`'s constructor, `free(): invalid pointer` right after (a corruption abort following the uncaught JS exception). Extracted the actual source from the compiled resource bundle to find the failing line (`gresource extract /usr/lib/gnome-shell/libshell-18.so /org/gnome/shell/misc/timeLimitsManager.js` -- gnome-shell's JS ships compiled into an ELF resource section inside `libshell-18.so`, not as loose files, so a plain `find` for the `.js` filename finds nothing).

  **Root cause**: that constructor calls `TimerChildProxy(Gio.DBus.system, 'org.freedesktop.MalcontentTimer1', ...)` to watch GNOME's parental-controls screen-time daemon. `Gio.DBus.system` is a property getter that opens the D-Bus *system* bus on first access and throws fatally if none is reachable at all -- a completely different failure mode from a *service* being absent on an open bus (`org.freedesktop.DBus.Error.ServiceUnknown`, which this same code handles gracefully via an error callback, same as several other missing-service warnings seen once past this point: GDM, logind, PolicyKit1, GeoClue2, colord). We'd only ever started a private *session* bus (matching the wrapper-PID fix already in `entrypoint.sh`/`test-crash.sh`); the system bus socket didn't exist in the container at all.

  **Fix**: start a second private bus the same way as the session one (`dbus-daemon --session --fork --print-address --print-pid`, launched directly, not via `dbus-run-session`) and export its address as `DBUS_SYSTEM_BUS_ADDRESS`. It doesn't need to behave like a real system bus -- no policy files, no actual logind/PolicyKit/GDM/GeoClue2/colord services -- gnome-shell already tolerates individual services being unreachable once the bus itself opens successfully. Confirmed live: with both buses present, gnome-shell survives past `timeLimitsManager.js`, creates its socket, and a real client (`gtk4-demo`) connects and stays alive.

  While debugging cleanup between attempts, re-learned a known mistake the hard way: `pkill -9 -f "gnome-shell"` run from inside a `bash -c '...pkill -9 -f "gnome-shell"...'` invocation matches and kills *itself*, because `pkill -f` matches the full cmdline substring and the pattern text is present in its own argv (the same self-matching class of bug `plan-test-harness.md`'s harness-tooling-follow-ups section already flags as "done ad hoc many times this session"). Symptom looked alarming (`sudo podman exec` exiting 137 with zero output) before the cause was obvious. Fixed by using `pkill -9 -x <exact-comm-name>` instead of `-f <substring>` for this kind of container-cleanup one-liner.

  L1 (manual crash+restart, `RUST_LOG=info`) surfaced two new globals gnome-shell advertises that no prior container did: `wp_commit_timing_manager_v1` (Gap 1 -- already generated by `wayland-protocols`' `staging` feature, same pattern as kwin's five; fixed with one alias + match arm) and `gtk_shell1` (Gap 2 -- a GNOME/GTK-internal protocol, startup notification/launch tracking, that never went through freedesktop's official process and isn't generated by any of the four crates this project draws from; added to `architecture-notes.md`'s Gap 2 list rather than chased, since there's nothing to wire up -- confirmed live it's safe to keep dropping, `gtk4-demo` never needs it and the full toplevel chain still comes up and survives a crash/reconnect cleanly).

  Re-verified live end to end after the `wp_commit_timing_manager_v1` fix: only `gtk_shell1` remains as an unresolvable-interface warning (expected, permanent Gap 2), `get_toplevel` reached, and a full crash+restart cycle recreated the whole toplevel chain (`wl_compositor`, `wl_surface`, `xdg_wm_base`, `xdg_surface`, `xdg_toplevel`) cleanly with the client alive throughout. `test-crash.sh` passed 8/8 across two batches (pre- and post-interfaces.rs-fix). `cargo test` (including a new `resolves_every_global_observed_from_real_mutter` regression test) passes.

- 2026-07-31 (later the same day): before starting the cross-compositor swap tests (the next item after mutter), built `scripts/run-registry.sh` -- a small sourced library tracking which pid/container/socket belongs to which role in a test run, motivated directly by two separate pain points hit earlier the same day: the self-matching `pkill -f "gnome-shell"` mistake during mutter cleanup (already flagged as a recurring class of bug in this file's own "Harness tooling follow-ups" section), and manually shuttling `DBUS_SESSION_BUS_ADDRESS` between separate `podman exec` calls during ad hoc debugging, since env vars don't survive across them. One directory per run under `$XDG_RUNTIME_DIR/wayland-proxy-runs/<run-id>/` (bind-mounted into every container at the same path, so it's visible from the host and any container -- required for the upcoming swap tests, which span two containers by design); a `current` symlink always points at the latest one; a `chain` file logs every tracked role/container/pid/socket as it's added.

  Building this surfaced three real correctness bugs in the mechanism itself, none of them hypothetical -- caught by actually exercising success, forced-failure, and gc paths live rather than trusting the design on paper:

  1. **First cut deleted the run directory unconditionally on exit**, including on failure -- exactly backwards, since a stuck/failed run is the one case this exists to help with. Fixed: `test-crash.sh`'s `cleanup()` now captures `$?` as the very first statement (before any other command resets it) and only `rm -rf`s the run directory on a clean exit; a failed run prints "Run directory kept for inspection" and leaves the chain/pid files in place.
  2. **Cross-container dispatch bug**: the initial rule for "can I signal this pid directly" was "am I inside any container" rather than "does the *recorded* container match my own" -- wrong the moment two containers' entries coexist in the same host-shared registry directory (they always do, since it's one bind-mounted directory, not one per container). Fixed by comparing the tracked container string against `${CONTAINER_ID:-HOST}` (my own actual context) rather than just checking whether `CONTAINER_ID` happens to be set at all; anything that doesn't match and isn't reachable via the host's `podman exec` now fails closed (treated as "can't verify," never as "confirmed dead") rather than guessed.
  3. **Pid reuse defeated garbage collection**: confirmed live that a killed compositor's pid got reused by an unrelated process inside the same busy container within about a second, so a bare `kill -0 <pid>` liveness check falsely reported "still alive" forever. Fixed by pairing every tracked pid with a `ps -o lstart=` timestamp captured at track time; a liveness check now requires both the pid to exist and its current `lstart` to still match. That alone wasn't sufficient either: **a zombie also still reports its original `lstart`**, and every one of this project's WM containers has pid 1 as a bare `sleep infinity` with nothing to reap children (the same already-known, already-accepted "harmless unreaped zombie" behaviour `diagnose.sh`'s own `count_zombie_labwc` separately documents) -- `dbus-daemon --fork` reliably leaves one behind on every run. Without also checking process state, gc could never collect a run containing one. Fixed by additionally requiring the process not be in state `Z` (`ps -o stat=`).

  A fourth, purely cosmetic bug also surfaced: `for dir in "$root"/*/` matches the `current` symlink too, since it points at a directory -- both `run_gc_stale_runs` and `diagnose.sh`'s new "run registry" section were iterating over it as if it were a second, distinct run (same content, no run-id, confusing "current:" label). Fixed by skipping a `run_id` of literally `current` in both loops. A related, smaller one: `entrypoint.sh`'s own `ls "$XDG_RUNTIME_DIR"/wayland-*` (unlike `test-crash.sh`/`diagnose.sh`'s equivalent listings, which already filter to `wayland-*[0-9]`) started matching the new `wayland-proxy-runs` directory name too -- fixed by aligning it with the digit-suffix convention the other two scripts already used.

  Wired into `test-crash.sh` (tracks compositor/proxy/client pids and sockets, plus both dbus-daemons for mutter) and `entrypoint.sh` (tracks the same, for the interactive/manual path -- no `run_cleanup` call there, since leaving the compositor running is the whole point; the registry exists purely for `cat`-ability during a manual session). `diagnose.sh` gained a "run registry" section (shown under both HOST and GUEST, same reasoning as its existing `print_wayland_sockets` -- same bind-mounted directory either way) and calls `run_gc_stale_runs` once per host-side invocation. Verified live end to end: a clean `test-crash.sh` run removes its own directory; a forced failure (bogus client binary) keeps it, with `chain` and per-role `.pid` files showing exactly the container/pid/socket history; a subsequent run's `run_dir_init` correctly garbage-collects the previous failed run once its pids (including the zombie dbus-daemons) are confirmed dead. `test-crash.sh` re-verified 3/3 clean passes against mutter afterward. `cargo test` and `shellcheck scripts/*.sh` both still pass.

  Before writing the swap tests themselves (the next item on the plan), extracted `scripts/compositor-launch.sh`: `test-crash.sh` and `entrypoint.sh` each carried an identical copy of the per-WM headless-launch case statement (kwin's `--virtual`, mutter's dual-bus dance), and the swap tests were about to need a third copy. Pulled into one `launch_compositor <wm> [log] [role]` function, sourced by all three; `[role]` (new, defaults to `"compositor"`) lets the swap script track two coexisting compositor identities (`compositor-sway`, `compositor-kwin`) in one shared run directory instead of the second overwriting the first. Re-verified live against sway, kwin, and mutter after the extraction -- no behavior change, same `test-crash.sh`/`entrypoint.sh` passes as before.

  **Cross-compositor swap tests, sway → kwin and kwin → mutter** (`plan-test-harness.md`'s remaining pairs): built `scripts/test-crash-swap.sh`, run from the host, orchestrating both containers via `podman exec -d` (detached exec) for the compositors and client, with the proxy itself running natively on the host (it has no GTK/GUI dependency, unlike the client, so it doesn't need a container at all). First real exercise of run-registry.sh's actual reason for existing: confirmed live via `ps -p <pid> -o pid,ppid,stat,cmd` that a plain `&`-backgrounded child of a `podman exec -d`'s top-level command survives that command exiting, reparented to the container's own pid 1 -- the mechanism the whole detached-launch approach depends on.

  Two real bugs before it ran clean, both mechanical:

  1. **Missing `$SCRIPT_DIR`**: `compositor-launch.sh`'s sway/labwc cases reference `"$SCRIPT_DIR/containers/<wm>/<wm>-config"`, relying on the caller having already set `$SCRIPT_DIR` -- true for `test-crash.sh`/`entrypoint.sh` (their own first line), but the swap script's `podman exec -d ... bash -c '...'` one-liners never did, so `$SCRIPT_DIR` was empty and the path resolved to the bare `/containers/sway/sway-config`. Fixed by setting `SCRIPT_DIR=/workspace/scripts` explicitly at the top of each remote one-liner.
  2. Verified the trickiest piece of shell quoting (`podman exec -d ... bash -c "... '$CLIENT_APP' ... \"\$!\" ..."`, mixing host-shell and container-shell expansion in one string) in isolation before trusting it in the full script -- confirmed live that `$CLIENT_APP` expands on the host (correct, it's a host-side variable) while `\$!` and `\"..\"` survive escaped through to the container's own bash, evaluating there against the container's own just-backgrounded pid (also correct, and the only way it could be -- `$!` on the host would be the `podman exec` wrapper, not the client).

  Both pairs passed 3/3 automated runs each, plus a manual `RUST_LOG=debug` pass per pair for L1-quality evidence (not just process survival): zero unresolvable-interface warnings, full toplevel chain recreated, socket reuse held across the container boundary in both directions. sway → kwin reproduced the exact same generic buffer/syncobj-churn warnings already root-caused for labwc → sway (`wp_linux_drm_syncobj_surface_v1.set_acquire_point`/`set_release_point`, `wl_surface.attach` referencing untranslatable objects) -- confirms that finding really was generic, not specific to any one WM pairing. kwin → mutter surfaced one new, non-blocking observation: gnome-shell's first accepted connection reset almost immediately (`state recovery after reconnect failed partway: Connection reset by peer`), likely a symptom of its heavier startup sequence relative to kwin/sway -- the proxy's existing reconnect-freeze logic (unchanged, no fix needed) correctly detected the resulting second EOF and completed recovery cleanly on the next attempt, `released keyboard grab on 32 before resuming traffic` and all. Read as reassuring evidence the recovery path tolerates a flakier real compositor, not a bug.

- 2026-07-31 (starting Phase 10, the automated matrix runner): the plan's own pass criteria says "L1 minimum -- L0-only doesn't count as verified," but `test-crash.sh` was, by its own documented design, L0-only -- it deliberately never restarts the compositor, so it never actually exercises reconnect/recreation. Every L1 verification this session (every "L1 pass" claim in this file and in `plan-test-harness.md`) was a manual `RUST_LOG=debug` run, eyeballed by hand. For an automated matrix runner, that's not automatable as-is.

  Added `test-crash.sh --l1`: after confirming the client survives the initial crash, restarts the SAME compositor on the same freed socket (reusing `launch_compositor`/`socket-wait.sh`, asserting the restart actually lands on the freed name, same check `test-crash-swap.sh` already does across containers) and asserts two things from the proxy's own log rather than eyeballing it: zero `"unresolvable interface"` warnings, and a `"recreated xdg_toplevel"` line proving the reconnect actually recreated the chain. Confirmed live that `tracing_subscriber::fmt::init()` shows INFO and above by default, so both of these are already in the log without needing `RUST_LOG` set explicitly. Default (no `--l1`) behavior is untouched, so every existing "L0 pass (N/N)" claim in this project's docs stays exactly what it says.

  First run against mutter failed the new check -- correctly, but for the wrong reason at first glance: `gtk_shell1`'s already-confirmed-safe, permanent Gap 2 unresolvable-interface warning tripped the blanket "any unresolvable interface = fail" rule. Fixed by excluding that one specific, already-documented case rather than loosening the check generally.

  Extracted `scripts/socket-wait.sh` (the `snapshot_live_sockets`/`wait_for_new_socket` pair, previously duplicated once already between `test-crash.sh` and `test-crash-swap.sh`) since `--l1` needed the same logic a second time within `test-crash.sh` itself -- a third near-identical copy would have been the same kind of drift risk `compositor-launch.sh` was extracted to avoid.

  A repeated-runs check (`--l1` five times in a row against mutter) surfaced a second, more serious bug, this time in the harness's own containers rather than the test script: runs 2 onward failed with `gtk4-demo` aborting outright (`fork: retry: Resource temporarily unavailable`, `GLib-GIO:ERROR: g_task_thread_pool_init: assertion failed: (task_pool != NULL)`). **Root cause**: every WM container's pid 1 is a bare `sleep infinity`, which never reaps terminated children (the same fact already noted as "harmless" for a handful of zombie `dbus-daemon`/labwc processes earlier in this file) -- but gnome-shell's own heavy child-process fan-out (gvfsd, evolution-data-server, dconf-service, goa-daemon, at-spi, xdg-desktop-portal, and more, all confirmed live via `ps aux` earlier this session) means every mutter test run leaves dozens of permanent zombies, not one or two. Five `--l1` runs accumulated 1483 zombies against the container's 2048 `pids-limit`, and the next `fork()` simply had nowhere left to go. Not cosmetic at scale, contrary to the earlier assumption.

  **Fix**: `setup-env.sh` now passes `--init` to `podman run`, installing Podman's own bundled subreaper (`podman-init`) as the container's real pid 1 instead of `sleep infinity` itself. Confirmed live in isolation first (`podman run --init ubuntu:26.04`, backgrounding a child and confirming zero zombies survive it, careful to avoid `ps aux | grep -c defunct`'s own self-matching false-positive -- used `ps -eo stat` and matched `^Z` instead), then applied by recreating the sway/kwin/mutter containers (teardown-env.sh + setup-env.sh) -- `ps -p 1` inside each now shows `podman-init`, and 5/5 repeated `--l1` runs against mutter afterward left zero zombies. labwc's container wasn't recreated (not currently needed for anything in flight), but the fix is captured in `setup-env.sh` for whenever it next is.
